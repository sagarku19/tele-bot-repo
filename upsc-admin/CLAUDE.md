<!--
  Single source of truth for admin-side agent rules is AGENTS.md (right here in this
  directory). This file is a stub so Claude Code, Cursor, and Codex all read identical
  content without us having to keep two copies in sync.
-->

# Admin agent rules → see `AGENTS.md`

All Next.js 16 / React 19 / Tailwind v4 gotchas for this admin codebase live in
[`AGENTS.md`](./AGENTS.md). Read that first.

For the wider project — bot side, Firestore schema, agent roles, log prefixes, known
limitations — see the repo-root [`CLAUDE.md`](../CLAUDE.md).
